class Solution:
    def minPairSum(self, nums):
        nums.sort()                 
        
        max_sum = 0
        n = len(nums)
        
        for i in range(n // 2):
            max_sum = max(max_sum, nums[i] + nums[n - 1 - i])
        
        return max_sum
