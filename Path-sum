class Solution:
    def hasPathSum(self, root: Optional[TreeNode], targetSum: int) -> bool:
        # If the tree is empty, there is no path
        if not root:
            return False
        
        # Check if we are at a leaf node
        if not root.left and not root.right:
            return targetSum == root.val
        
        # Recursive case: Subtract the current value and check children
        new_sum = targetSum - root.val
        return self.hasPathSum(root.left, new_sum) or self.hasPathSum(root.right, new_sum)
